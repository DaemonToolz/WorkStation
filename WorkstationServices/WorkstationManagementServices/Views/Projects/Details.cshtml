@model WorkstationManagementServices.Models.Database.Project

@{
    ViewBag.Title = "Details";
}

<h2> @Html.DisplayFor(model => model.name) </h2>

<div>
    <h4>Project</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Project
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt>
            Root
        </dt>

        <dd>
            @Html.DisplayFor(model => model.root)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink(" ", "Edit", new { id = Model.id }, new { @class = "glyphicon glyphicon-cog" }) |
    @Html.ActionLink(" ", "Index", new { }, new { @class = "glyphicon glyphicon-th-list" })
</p>
